{
  "name": "kubeflare",
  "version": "0.1.0",
  "private": true,
  "description": "A real Kubernetes cluster on a Cloudflare Container",
  "license": "MIT",
  "scripts": {
    "deploy": "./scripts/deploy.sh",
    "teardown": "./scripts/teardown.sh",
    "preflight": "./scripts/preflight.sh"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
