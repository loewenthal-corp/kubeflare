node_modules/
.wrangler/
.dev.vars

# Credentials — kubeconfig.yaml embeds a cluster-admin token
.env
.env.*
kubeconfig.yaml
tunnel-kubeconfig.yaml
*.token
*_ed25519
*.pem

.DS_Store
*.log
.kubeflare-token
.kubeflare-k3s-token
.kubeflare-node-password

# stray go build output
svcproxy/svcproxy

# image-cache registry credentials
.kubeflare-registry-creds

# tunnel id (not secret, but environment-specific)
.kubeflare-tunnel-id
